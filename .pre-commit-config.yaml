# Сканирование секретов перед коммитом (gitleaks).
# Блокирует commit, если в staged-файлах найден ключ / токен / пароль.
# Установка хука: pre-commit install
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
